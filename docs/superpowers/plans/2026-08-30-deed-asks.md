# 행동으로 푸는 요청 (deed asks) — 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 메신저 요청을 타이핑 대신 앱에서의 행동(셀 저장·메일·파일 이동/이름·드라이브 업로드)으로 완료할 수 있게 하고, 그런 요청 16건을 pool에 더한다.

**Architecture:** ask에 `deed: '<objective id>'`를 두면 그 ask는 텍스트 대신 그 grant를 기다린다. 검사는 objective에 산다(`cell`은 있음; `mail`·`move`·`rename`·`upload` 추가). `store.grant(key)`가 grant를 켠 뒤 그 key를 기다리던 pending ask를 찾아 `ok`를 말하고 `then`으로 넘긴다. 새 상태 `placed`·`uploaded`는 PROGRESS에 실린다.

**Tech Stack:** React 18 · zustand 5 · Vite 6 · Vitest 2. 콘텐츠는 `src/scenarios/workday.json` (node 스크립트로만 수정).

Spec: `docs/superpowers/specs/2026-08-30-deed-asks-design.md`

## Global Constraints

- **workday.json을 Read/cat 하지 마라.** 조회는 `node scripts/query.mjs <명령>`, 수정은 node 스크립트(`JSON.parse` → 수정 → `JSON.stringify(s,null,2)+'\n'`).
- **JS/JSX는 CRLF.** 파이썬으로 고칠 땐 `\r\n→\n` 정규화 후 수정, 저장 시 `\n→\r\n`. Edit 도구를 쓰면 신경 안 써도 된다.
- `store.js`(80KB)·`shell.css`(125KB)는 통째로 읽지 말고 `grep -n`으로 찾아 offset/limit로 읽어라.
- 테스트: 관련 파일만 `npx vitest run test/<x>.test.js`, 태스크 끝에 한 번 `npm test`. `npm run build`가 JSX 문법 검증.
- 새 진행 상태는 반드시 `store.js`의 `PROGRESS` 배열에도 넣는다.
- 새 대사에 정답 문자열(accept) 금지. 힌트 `no`는 3단계, 마지막 단계는 폴더/파일/페이지/셀을 정확히 지목. `ok`/`placeholder`/`next:[]` 필수.
- pool 요청은 `objectives`에 `id === grant`인 항목이 있어야 하고, 마지막 단계의 `grants === 요청 id` (`test/pool.test.js`).
- 커밋 메시지는 한국어, 끝에 `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`. 브랜치 `deed-asks`에서 작업.

---

## 파일 지도

| 파일 | 책임 |
|---|---|
| `src/engine/store.js` | `finishDeeds`, `placed`/`uploaded` 상태와 액션, `fsView`의 `place()`, `sendMail` 후보 확장, `checkPlaced`/`checkUploaded` |
| `src/engine/goal.js` | `checkOutbound`에 첨부 검사, `rudeReplies` 없는 스펙은 예절로 막지 않음 |
| `src/apps/Messenger.jsx` | deed ask는 입력창 대신 안내문 |
| `src/apps/FileExplorer.jsx` | 우클릭 메뉴 잘라내기·이름 바꾸기·붙여넣기, 인라인 이름 입력 |
| `src/apps/Drive.jsx` | 페이지 상단 업로드 버튼, 올린 파일 표 |
| `src/apps/FileDialog.jsx`, `Viewer.jsx`, `shell/Desktop.jsx` | `fsView`에 `placed` 전달 |
| `src/shell/shell.css` | `.quick-deed`, `.ex-rename`, `.dr-upload`, `.dr-uploaded` |
| `test/deed.test.js` (신규) | 엔진 4종 + 스펙 불변식 |
| `test/scenario.test.js`, `test/hintAsk.test.js` | deed ask 면제 |
| `scripts/query.mjs`, `CLAUDE.md` | deed 표기·규칙 한 줄 |

---

### Task 1: `ask.deed` — grant를 기다리는 질문

**Files:**
- Modify: `src/engine/store.js` (`grant:` 액션, 그 아래에 `finishDeeds` 추가)
- Modify: `src/apps/Messenger.jsx` (quick 영역 분기)
- Modify: `src/shell/shell.css` (끝에 추가)
- Modify: `test/hintAsk.test.js:13`
- Create: `test/deed.test.js`

**Interfaces:**
- Produces: `store.finishDeeds(key)` — `pendingAsks` 중 `ask.deed === key`인 스레드를 해결. `grant(key)`가 마지막에 부른다.
- ask 모양: `{ deed: '<objective id>', placeholder, ok, no, next: [], grants? , then? }`.

- [ ] **Step 1: 실패하는 테스트**

`test/deed.test.js`:

```js
import { beforeEach, describe, expect, it, vi } from 'vitest'
import scenario from '../src/scenarios/workday.json'
import { useGame } from '../src/engine/store.js'

// 요청의 86%가 '찾아서 타이핑'이었다. 행동으로 푸는 요청은 ask가 텍스트
// 대신 grant를 기다린다 — 그 행동이 일어나면 창이 닫혀 있어도 답이 온다.
const DEED = { deed: 'orders', placeholder: '시트를 고쳐 저장하면 확인됩니다', ok: ['고쳤네요, 감사합니다.'], no: [['a'], ['b'], ['c']], next: [] }

describe('행동을 기다리는 질문', () => {
  beforeEach(() => {
    vi.useFakeTimers()
    useGame.setState({ grants: {}, pendingAsks: {}, extraMessages: {}, windows: [], openThread: {}, beatQueue: [], beatAsk: null, branches: {}, day: 2 })
  })

  it('그 grant가 켜지면 ok를 말하고 질문이 닫힌다', () => {
    useGame.setState({ pendingAsks: { boss: DEED } })
    useGame.getState().grant('orders')
    vi.runAllTimers()
    expect(useGame.getState().pendingAsks.boss).toBe(null)
    expect(useGame.getState().extraMessages.boss.map((m) => m.text)).toEqual(DEED.ok)
  })

  it('다른 grant에는 꿈쩍하지 않는다', () => {
    useGame.setState({ pendingAsks: { boss: DEED } })
    useGame.getState().grant('wifi')
    vi.runAllTimers()
    expect(useGame.getState().pendingAsks.boss).toBe(DEED)
    expect(useGame.getState().extraMessages.boss).toBeUndefined()
  })

  it('체인 가운데의 행동은 다음 질문으로 넘긴다', () => {
    const next = { placeholder: '다음', accept: ['x'], ok: ['끝'], no: [['a'], ['b'], ['c']], next: [] }
    useGame.setState({ pendingAsks: { boss: { ...DEED, then: next } } })
    useGame.getState().grant('orders')
    vi.runAllTimers()
    expect(useGame.getState().pendingAsks.boss).toBe(next)
  })

  it('마지막 단계의 grants도 같이 켠다', () => {
    useGame.setState({ pendingAsks: { boss: { ...DEED, grants: 'orders' } } })
    useGame.getState().grant('orders')
    vi.runAllTimers()
    expect(useGame.getState().grants.orders).toBe(true)
    // 두 번 켜지지 않는다 — ok 대사가 두 번 오면 그게 증거다
    expect(useGame.getState().extraMessages.boss.map((m) => m.text)).toEqual(DEED.ok)
  })
})
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run test/deed.test.js`
Expected: 4 failed — `pendingAsks.boss` still `DEED`, `extraMessages.boss` undefined.

- [ ] **Step 3: store에 `finishDeeds`**

`src/engine/store.js`의 `grant:` 액션에서 `get().chat(key)` 바로 다음 줄에:

```js
    get().chat(key)
    get().finishDeeds(key)
```

그리고 `grant:` 액션이 끝나는 `},` 바로 뒤에 추가:

```js
  // 행동을 기다리던 질문. 그 행동의 grant가 켜지면 여기서 답이 온다 —
  // 대화는 게임의 것이라 메신저 창이 닫혀 있어도 진행된다. 마지막 단계가
  // 든 grants는 보통 deed와 같은 키라 이미 켜져 있고, 다르면 여기서 켠다.
  finishDeeds: (key) => {
    const s = get()
    for (const [threadId, ask] of Object.entries(s.pendingAsks)) {
      if (!ask || ask.deed !== key) continue
      const t = allThreads(s.scenario).find((x) => x.id === threadId)
      get().setAsk(threadId, ask.then ?? null)
      if (ask.next) get().setBranch(threadId, ask.next)
      if (ask.grants && !get().grants[ask.grants]) get().grant(ask.grants)
      get().saying(threadId, t?.name ?? '', ask.ok ?? [])
    }
  },
```

- [ ] **Step 4: 통과 확인**

Run: `npx vitest run test/deed.test.js`
Expected: 4 passed.

- [ ] **Step 5: 메신저 — deed면 입력창 대신 안내문**

`src/apps/Messenger.jsx`의 quick 영역, `) : ask ? (` 분기 **앞**에 넣는다:

```jsx
              ) : ask?.deed ? (
                // 행동으로 푸는 질문. 칠 것이 없다 — 무엇을 하면 되는지만 남긴다.
                <span className="quick-done quick-deed">{ask.placeholder}</span>
              ) : ask ? (
```

`src/shell/shell.css` 끝에:

```css
/* 행동으로 푸는 질문: 입력칸 자리에 안내문만 */
.quick-deed { color: #6b7280; font-style: italic; }
```

- [ ] **Step 6: 힌트 버튼 검사가 deed도 세게**

`test/hintAsk.test.js:13`의

```js
    if (n.no?.length && (n.accept || n.files || n.free)) asks.push(n)
```
을
```js
    if (n.no?.length && (n.accept || n.files || n.free || n.deed)) asks.push(n)
```

- [ ] **Step 7: 빌드·관련 테스트**

Run: `npm run build && npx vitest run test/deed.test.js test/hintAsk.test.js test/saying.test.js`
Expected: build ✓, all passed.

- [ ] **Step 8: 커밋**

```bash
git add src/engine/store.js src/apps/Messenger.jsx src/shell/shell.css test/deed.test.js test/hintAsk.test.js
git commit -m "feat: 행동을 기다리는 질문 (ask.deed)

요청의 86%가 '찾아서 타이핑'이었다. ask가 deed 로 objective 를 가리키면
텍스트 대신 그 grant 를 기다리고, 켜지는 순간 창이 닫혀 있어도 답이 온다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: 메일 목표 — 요청 단위로 보내는 메일

**Files:**
- Modify: `src/engine/goal.js:21-40` (`checkOutbound`)
- Modify: `src/engine/store.js` (`sendMail:` 액션)
- Modify: `test/deed.test.js` (describe 추가)

**Interfaces:**
- objective 스펙: `mail: { to, requiredAttachment?, requiredKeywords?, reply, wrongAttachmentReply?, unclearReply? }`. `reply`는 `{from, subject, body, attach?}` (fetch의 것과 같은 모양, `{subject}` 치환).
- `checkOutbound(spec, {to, subject, body, attachmentId}, etiquette, player)` — `spec.rudeReplies`가 없으면 예절로 막지 않는다(대신 호출자가 `scold`).
- `sendMail({to, subject, body, attachmentId})` — 후보 = 그날 fetch + 아직 안 켜진 `mail` objective들.

- [ ] **Step 1: 실패하는 테스트**

`test/deed.test.js` 끝에:

```js
// 새 메일 쓰기는 그날의 fetch 하나만 알았다. 이제 아직 안 켜진 메일 목표
// 전부가 후보다 — 수신자가 맞는 것을 고르고, 첨부·키워드·예절을 본다.
describe('메일로 푸는 요청', () => {
  const SPEC = {
    id: 'deed_t_mail', title: 't', grant: 'deed_t_mail',
    mail: {
      to: 'ym.kim@ctech.co.kr', requiredAttachment: 'file_qc', requiredKeywords: ['견적서'],
      reply: { from: '김영민 <ym.kim@ctech.co.kr>', subject: 'RE: {subject}', body: '받았습니다.' },
      wrongAttachmentReply: { from: '김영민 <ym.kim@ctech.co.kr>', subject: 'RE: {subject}', body: '첨부가 다릅니다.' },
      unclearReply: { from: '김영민 <ym.kim@ctech.co.kr>', subject: 'RE: {subject}', body: '무엇을 보내신 건지요.' }
    }
  }
  const polite = '안녕하세요, AR주식회사 김한별 대리입니다.\n요청하신 견적서 첨부합니다.\n감사합니다.\n김한별 드림'
  const withSpec = () => useGame.setState({
    scenario: { ...scenario, objectives: [...scenario.objectives, SPEC] },
    grants: {}, extraMails: [], pendingAsks: { boss: { deed: 'deed_t_mail', placeholder: 'p', ok: ['보냈군요.'], no: [['a'], ['b'], ['c']], next: [] } },
    extraMessages: {}, windows: [], openThread: {}, day: 2
  })
  beforeEach(() => { vi.useFakeTimers(); withSpec() })

  it('수신자·첨부·키워드가 맞으면 답장이 오고 요청이 닫힌다', () => {
    const ok = useGame.getState().sendMail({ to: 'ym.kim@ctech.co.kr', subject: '[AR주식회사] 견적서 송부', body: polite, attachmentId: 'file_qc' })
    expect(ok).toBe(true)
    vi.runAllTimers()
    expect(useGame.getState().extraMails.at(-1).body).toBe('받았습니다.')
    expect(useGame.getState().grants.deed_t_mail).toBe(true)
    expect(useGame.getState().pendingAsks.boss).toBe(null)
  })

  it('첨부가 틀리면 그 답장이 오고 요청은 그대로다', () => {
    const ok = useGame.getState().sendMail({ to: 'ym.kim@ctech.co.kr', subject: '[AR주식회사] 견적서', body: polite, attachmentId: 'file_qb' })
    expect(ok).toBe(false)
    vi.runAllTimers()
    expect(useGame.getState().extraMails.at(-1).body).toBe('첨부가 다릅니다.')
    expect(useGame.getState().grants.deed_t_mail).toBeUndefined()
    expect(useGame.getState().pendingAsks.boss).not.toBe(null)
  })

  it('아무 후보도 아닌 주소는 되돌아온다', () => {
    useGame.getState().sendMail({ to: 'nobody@nowhere.kr', subject: 'x', body: polite, attachmentId: null })
    vi.runAllTimers()
    expect(useGame.getState().extraMails.at(-1).from).toContain('mailer-daemon')
  })

  it('그날의 fetch는 예전처럼 그대로 된다', () => {
    useGame.setState({ day: 3 })
    const f = scenario.days[2].fetch
    const body = `안녕하세요, AR주식회사 김한별 대리입니다.\n${f.requiredKeywords[0]} 자료 부탁드립니다.\n감사합니다.\n김한별 드림`
    expect(useGame.getState().sendMail({ to: f.to, subject: '[AR주식회사] 자료 요청', body, attachmentId: null })).toBe(true)
  })
})
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run test/deed.test.js`
Expected: 첫 두 개 실패 (`sendMail`이 `attachmentId`를 모르고 objective를 안 본다). 셋째·넷째는 통과할 수 있다.

- [ ] **Step 3: `checkOutbound`에 첨부, 예절은 스펙이 요구할 때만**

`src/engine/goal.js`의 `checkOutbound` 전체를 이걸로:

```js
export function checkOutbound(fetch, { to, subject, body, attachmentId = null }, etiquette, player) {
  if (!fetch || norm(to).toLowerCase() !== norm(fetch.to).toLowerCase()) {
    return { ok: false, reason: 'address', reply: null }
  }
  // 이 메일은 본퀘스트다. 답장과 달리 예절을 어기면 되돌아온다 — 되돌릴 말을
  // 가진 스펙(그날의 fetch)만. 요청 단위의 메일 목표는 잔소리로 갈음한다.
  if (fetch.rudeReplies) {
    const [rude] = checkEtiquette(
      { ...etiquette, company: player.company, name: player.name },
      { subject, body, outbound: true }
    )
    if (rude) return { ok: false, reason: rude, reply: fetch.rudeReplies[rude] }
  }
  if (fetch.requiredAttachment && attachmentId !== fetch.requiredAttachment) {
    return { ok: false, reason: 'attachment', reply: fetch.wrongAttachmentReply }
  }
  if ((fetch.requiredKeywords ?? []).some((k) => !norm(body).includes(norm(k)))) {
    return { ok: false, reason: 'keyword', reply: fetch.unclearReply }
  }
  return { ok: true, reason: null, reply: fetch.reply }
}
```

- [ ] **Step 4: `sendMail`이 목표들을 본다**

`src/engine/store.js`의 `sendMail:` 액션 전체를 이걸로 (grep `sendMail: ({ to, subject, body })`):

```js
  sendMail: ({ to, subject, body, attachmentId = null }) => {
    const s = get()
    // 그날의 fetch 하나만 보던 것을 아직 안 켜진 메일 목표 전부로 넓힌다.
    // 수신자가 맞는 첫 후보가 이 메일의 상대다. 없으면 되돌아온다.
    const specs = [
      s.scenario.days[s.day - 1]?.fetch,
      ...s.scenario.objectives.filter((o) => o.mail && !s.grants[o.grant]).map((o) => ({ ...o.mail, grants: o.grant }))
    ].filter(Boolean)
    const same = (a, b) => String(a).replace(/\s/g, '').toLowerCase() === String(b).replace(/\s/g, '').toLowerCase()
    const spec = specs.find((f) => same(f.to, to)) ?? null
    const verdict = checkOutbound(spec, { to, subject, body, attachmentId }, s.scenario.etiquette, s.scenario.player)
    // 요청 단위의 메일은 예절로 막지 않는다. 일은 되고, 잠시 뒤 팀장이 한마디 한다.
    if (verdict.ok && !spec.rudeReplies) get().scold({ subject, body, outbound: true })
    const fill = (t = '') => t.replace('{to}', to).replace('{subject}', subject)
    const reply = verdict.reply ?? s.scenario.goal.bounce
    setTimeout(() => {
      const mail = { ...reply, id: 'in_' + Date.now(), date: '방금', at: justNow(s.scenario, s.day), subject: fill(reply.subject), body: fill(reply.body) }
      set((st) => ({ extraMails: [...st.extraMails, mail] }))
      get().showToast({ from: mail.from, text: `새 메일이 도착했습니다: ${mail.subject}`, app: 'mail' })
      if (verdict.ok) setTimeout(() => get().grant(spec.grants), 2200)
      const nags = s.scenario.etiquette.nags[verdict.reason]
      if (nags) get().nag(nags)
    }, 1800)
    return verdict.ok
  }
```

`scold`의 서명은 `scold({ subject, body, outbound })` — 그대로 쓴다.

- [ ] **Step 5: 통과 확인**

Run: `npx vitest run test/deed.test.js test/mail.test.js test/fetch.test.js 2>/dev/null || npx vitest run test/deed.test.js`
그 뒤 `npm test`. Expected: 모두 통과. (fetch 관련 기존 테스트 이름이 다르면 `npm test`로 잡는다.)

- [ ] **Step 6: 커밋**

```bash
git add src/engine/goal.js src/engine/store.js test/deed.test.js
git commit -m "feat: 요청 단위의 메일 목표

새 메일 쓰기가 그날의 fetch 하나만 알았다. 아직 안 켜진 mail 목표 전부를
후보로 두고 수신자가 맞는 것을 고른다. 첨부도 본다. 예절은 되돌릴 말이
있는 fetch 만 막고, 요청 단위 메일은 팀장의 잔소리로 갈음한다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: 파일 옮기기·이름 바꾸기

**Files:**
- Modify: `src/engine/store.js` (`PROGRESS`, 상태 초기화, 액션 3개 + `checkPlaced`, `fsView`/`place`)
- Modify: `src/apps/FileExplorer.jsx`, `src/apps/FileDialog.jsx:19`, `src/apps/Viewer.jsx:26`, `src/shell/Desktop.jsx:25`
- Modify: `src/shell/shell.css`
- Modify: `test/deed.test.js`

**Interfaces:**
- 상태 `placed: { [fileId]: { into?: '문서/업무자료/2026/C테크', name?: '새이름.hwp' } }` (PROGRESS), `clipboard: fileId|null` (저장 안 함).
- `placeFile(fileId, into)`, `renameFile(fileId, name)`, `cutFile(fileId)`, `checkPlaced()`.
- `fsView(fs, { pinned, restored, tiles, placed, scenario })` — `placed` 인자 추가. `place(fs, placed)` export.
- objective 스펙: `move: { file, into }`, `rename: { file, name }`.

- [ ] **Step 1: 실패하는 테스트**

`test/deed.test.js` 상단 import를 `import { fsView, place, useGame } from '../src/engine/store.js'` 로 바꾸고 끝에:

```js
// 옮기거나 이름을 바꾼 파일은 뷰에서 그 자리에 있다. id는 그대로라 첨부와
// 목표와 힌트는 아무것도 눈치채지 못한다 — 그래야 옮긴 뒤에도 게임이 산다.
describe('파일을 옮기고 이름을 바꾼다', () => {
  const at = (fs, path) => path.slice(1).reduce((es, n) => es.find((e) => e.name === n)?.children ?? [], fs[path[0]] ?? [])
  const ids = (es) => es.filter((e) => !e.children).map((e) => e.id)

  it('옮기면 원래 자리에서 빠지고 목적 폴더에 들어간다', () => {
    const fs = place(scenario.fs, { file_ppt_meeting: { into: '문서/발표자료' } })
    expect(ids(at(fs, ['다운로드']))).not.toContain('file_ppt_meeting')
    expect(ids(at(fs, ['문서', '발표자료']))).toContain('file_ppt_meeting')
  })

  it('없는 폴더로는 안 옮긴다', () => {
    const fs = place(scenario.fs, { file_ppt_meeting: { into: '문서/없는폴더' } })
    expect(ids(at(fs, ['다운로드']))).toContain('file_ppt_meeting')
  })

  it('이름을 바꿔도 id는 그대로다', () => {
    const fs = place(scenario.fs, { file_dummy_082: { name: '회의자료_0825.pptx' } })
    const f = at(fs, ['다운로드']).find((e) => e.id === 'file_dummy_082')
    expect(f.name).toBe('회의자료_0825.pptx')
  })

  it('fsView가 placed를 받는다', () => {
    const fs = fsView(scenario.fs, { placed: { file_ppt_meeting: { into: '문서/발표자료' } }, scenario })
    expect(ids(at(fs, ['문서', '발표자료']))).toContain('file_ppt_meeting')
  })

  it('목표가 가리키는 자리로 옮기면 grant, 다른 데면 침묵', () => {
    vi.useFakeTimers()
    const SPEC = { id: 'deed_t_move', title: 't', grant: 'deed_t_move', move: { file: 'file_ppt_meeting', into: '문서/발표자료' } }
    useGame.setState({ scenario: { ...scenario, objectives: [...scenario.objectives, SPEC] }, grants: {}, placed: {}, pendingAsks: {}, extraMessages: {} })
    useGame.getState().placeFile('file_ppt_meeting', '문서/개인')
    expect(useGame.getState().grants.deed_t_move).toBeUndefined()
    useGame.getState().placeFile('file_ppt_meeting', '문서/발표자료')
    expect(useGame.getState().grants.deed_t_move).toBe(true)
  })

  it('이름 목표도 같다', () => {
    const SPEC = { id: 'deed_t_rename', title: 't', grant: 'deed_t_rename', rename: { file: 'file_dummy_082', name: '회의자료_0825.pptx' } }
    useGame.setState({ scenario: { ...scenario, objectives: [...scenario.objectives, SPEC] }, grants: {}, placed: {}, pendingAsks: {}, extraMessages: {} })
    useGame.getState().renameFile('file_dummy_082', '회의자료_0825.pptx')
    expect(useGame.getState().grants.deed_t_rename).toBe(true)
  })

  it('placed는 세이브에 실린다', async () => {
    const { PROGRESS } = await import('../src/engine/store.js')
    expect(PROGRESS).toContain('placed')
  })
})
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run test/deed.test.js`
Expected: `place is not a function` 등으로 새 describe 전부 실패.

- [ ] **Step 3: store — 상태·액션·`place`**

`PROGRESS` 배열 끝 `'sealed', 'frozen']` → `'sealed', 'frozen', 'placed', 'uploaded']`.

상태 초기화: `dreamt: restored?.dreamt ?? false,` 위에

```js
  // 옮기거나 이름을 바꾼 파일. 뷰(fsView)가 적용한다 — 원본 트리는 그대로다.
  placed: restored?.placed ?? {},
  // 드라이브 페이지에 올린 파일 id들. Task 4 가 쓴다.
  uploaded: restored?.uploaded ?? {},
  // 잘라낸 파일. 세이브에는 안 실린다.
  clipboard: null,
```

액션 — `checkCells:` 액션 위에:

```js
  cutFile: (fileId) => set({ clipboard: fileId }),
  placeFile: (fileId, into) => {
    set((s) => ({ placed: { ...s.placed, [fileId]: { ...s.placed[fileId], into } }, clipboard: null }))
    get().checkPlaced()
  },
  renameFile: (fileId, name) => {
    set((s) => ({ placed: { ...s.placed, [fileId]: { ...s.placed[fileId], name } } }))
    get().checkPlaced()
  },
  // 옮기기·이름 목표. 셀 목표와 같은 식이다 — 자리가 맞는 순간 켜진다.
  checkPlaced: () => {
    const { scenario, placed, grants, grant } = get()
    scenario.objectives
      .filter((o) => !grants[o.grant] && (
        (o.move && placed[o.move.file]?.into === o.move.into) ||
        (o.rename && placed[o.rename.file]?.name === o.rename.name)))
      .forEach((o) => grant(o.grant))
  },
```

`fsView`를 이렇게 바꾼다:

```js
export function fsView(fs, { pinned = [], restored = {}, tiles = [], placed = {}, scenario } = {}) {
  const binned = []
  const strip = (entries) => entries.flatMap((e) => {
    if (e.children) return [{ ...e, children: strip(e.children) }]
    if (e.deleted && !restored[e.id]) { binned.push(e); return [] }
    // a mail attachment is nowhere until it is saved from the mail
    if (e.attached && !restored[e.id]) return []
    return [e]
  })
  const out = Object.fromEntries(Object.entries(fs).map(([root, entries]) => [root, strip(entries)]))
  out['휴지통'] = [...(out['휴지통'] ?? []), ...binned]
  return fsWithTiles(scenario, fsWithPinned(place(out, placed), pinned), tiles)
}

// 옮기거나 이름을 바꾼 파일. 원래 자리에서 빼서 목적 폴더 끝에 넣는다 —
// 목적 폴더가 없으면 그 자리에 둔다. id는 그대로라 첨부·목표·힌트는 아무것도
// 눈치채지 못한다. 트리는 새로 만들어 돌려주므로 원본은 손대지 않는다.
export function place(fs, placed = {}) {
  if (!Object.keys(placed).length) return fs
  const folderAt = (tree, path) => path.slice(1).reduce(
    (es, name) => es?.find((e) => e.children && e.name === name)?.children ?? null,
    tree[path[0]] ?? null)
  const moving = []
  const pull = (entries) => entries.flatMap((e) => {
    if (e.children) return [{ ...e, children: pull(e.children) }]
    const p = placed[e.id]
    if (!p) return [e]
    const named = p.name ? { ...e, name: p.name } : e
    if (!p.into || !folderAt(fs, p.into.split('/'))) return [named]
    moving.push([p.into, named])
    return []
  })
  const out = Object.fromEntries(Object.entries(fs).map(([root, entries]) => [root, pull(entries)]))
  for (const [into, file] of moving) folderAt(out, into.split('/')).push(file)
  return out
}
```

`fsView` 호출 네 곳에 `placed` 전달 — 각 파일에서 `const placed = useGame((s) => s.placed)`를 추가하고 호출을 `fsView(..., { pinned, restored, tiles, placed, scenario })`로:
- `src/apps/FileExplorer.jsx:26`
- `src/apps/FileDialog.jsx:19`
- `src/apps/Viewer.jsx:26`
- `src/shell/Desktop.jsx:25` (`fsView(scenario.fs, { pinned, restored, tiles, placed, scenario })`)

- [ ] **Step 4: 통과 확인**

Run: `npx vitest run test/deed.test.js test/hidden.test.js test/scenario.test.js`
Expected: 모두 통과.

- [ ] **Step 5: 탐색기 UI — 잘라내기 · 이름 바꾸기 · 붙여넣기**

`src/apps/FileExplorer.jsx`:

상단 셀렉터에 추가:
```js
  const placed = useGame((s) => s.placed)
  const clipboard = useGame((s) => s.clipboard)
  const cutFile = useGame((s) => s.cutFile)
  const placeFile = useGame((s) => s.placeFile)
  const renameFile = useGame((s) => s.renameFile)
  const [renaming, setRenaming] = useState(null)   // { id, value }
```

`onContext` 아래에 폴더 빈자리용 메뉴와 붙여넣기:
```js
  // 빈자리 우클릭: 잘라낸 것이 있을 때만 붙여넣기 메뉴가 뜬다.
  const onBlank = (e) => {
    if (e.target !== e.currentTarget || !clipboard) return
    e.preventDefault()
    const box = e.currentTarget.closest('.ex-body').getBoundingClientRect()
    setMenu({ paste: true, x: e.clientX - box.left, y: e.clientY - box.top })
  }
  // 휴지통과 작업 폴더에는 붙여 넣지 않는다 — 하나는 지운 것, 하나는 복사본이다.
  const canPaste = !inTrash && !inWork && !searching
  const paste = () => { placeFile(clipboard, nav.path.join('/')); setMenu(null) }
  // 이름 바꾸기는 본문만. 확장자가 바뀌면 여는 앱이 바뀌어 버린다.
  const ext = (name) => name.slice(name.lastIndexOf('.'))
  const commitRename = () => {
    const v = renaming.value.trim()
    if (v) renameFile(renaming.id, v + ext(renaming.name))
    setRenaming(null)
  }
```

`.ex-main` div에 `onContextMenu={onBlank}` 추가.

파일 버튼(검색 결과 아닌 `files.map` 쪽)의 이름 자리 `{f.name}` 을 이걸로:
```jsx
                  {renaming?.id === f.id ? (
                    <input className="ex-rename" autoFocus value={renaming.value}
                           onChange={(e) => setRenaming({ ...renaming, value: e.target.value })}
                           onKeyDown={(e) => { if (e.key === 'Enter') commitRename(); if (e.key === 'Escape') setRenaming(null) }}
                           onBlur={commitRename} onClick={(e) => e.stopPropagation()} />
                  ) : f.name}
```

메뉴 렌더: 기존 `{menu && (...)}` 블록을 이걸로:
```jsx
        {menu && (
          <>
            <div className="ctx-catch" onPointerDown={() => setMenu(null)}
                 onContextMenu={(e) => { e.preventDefault(); setMenu(null) }} />
            <div className="ctx" style={{ left: menu.x, top: menu.y }}>
              {menu.paste ? (
                <button disabled={!canPaste} onClick={paste}>붙여넣기</button>
              ) : (
                <>
                  <button onClick={() => { openWindow(fileOpener(menu.file).app, { fileId: menu.file.id }); setMenu(null) }}>
                    열기
                  </button>
                  {inTrash ? (
                    menu.file.deleted && (
                      <button onClick={() => { restoreFile(menu.file.id); setMenu(null) }}>복원</button>
                    )
                  ) : inWork ? (
                    <button onClick={() => { unpinFile(menu.file.id); setMenu(null) }}>
                      {WORK_FOLDER}에서 빼기
                    </button>
                  ) : (
                    <>
                      <button onClick={() => { pinFile(menu.file.id); setMenu(null) }}>
                        {WORK_FOLDER}에 복사
                      </button>
                      <button onClick={() => { cutFile(menu.file.id); setMenu(null) }}>잘라내기</button>
                      <button onClick={() => {
                        const n = menu.file.name
                        setRenaming({ id: menu.file.id, name: n, value: n.slice(0, n.lastIndexOf('.')) })
                        setMenu(null)
                      }}>이름 바꾸기</button>
                    </>
                  )}
                </>
              )}
            </div>
          </>
        )}
```

잘라낸 파일은 흐리게: 파일 버튼 className을 `'ex-file' + (clipboard === f.id ? ' cut' : '')`.

`src/shell/shell.css` 끝에:
```css
/* 탐색기: 잘라낸 파일과 이름 바꾸기 입력 */
.ex-file.cut { opacity: .5; }
.ex-rename { width: 100%; font: inherit; font-size: 12px; text-align: center; padding: 1px 3px; border: 1px solid #0078d4; border-radius: 2px; outline: none; }
```

- [ ] **Step 6: 빌드·수동 확인·전체 테스트**

Run: `npm run build && npm test`
Expected: build ✓, all passed. 개발 서버에서 다운로드의 파일 하나를 잘라내 문서/발표자료에 붙여넣고, 이름을 바꿔 보고, 새로고침 뒤에도 남아 있는지 본다.

- [ ] **Step 7: 커밋**

```bash
git add src/engine/store.js src/apps/FileExplorer.jsx src/apps/FileDialog.jsx src/apps/Viewer.jsx src/shell/Desktop.jsx src/shell/shell.css test/deed.test.js
git commit -m "feat: 파일을 옮기고 이름을 바꾼다

탐색기에 잘라내기·붙여넣기·이름 바꾸기. 옮긴 자리는 placed 에 남고
fsView 가 적용한다 — id 는 그대로라 첨부·목표·힌트는 눈치채지 못한다.
move/rename 목표는 셀 목표처럼 자리가 맞는 순간 켜진다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: 사내 드라이브에 올리기

**Files:**
- Modify: `src/engine/store.js` (액션 `uploadTo`, `checkUploaded`)
- Modify: `src/apps/Drive.jsx`
- Modify: `src/shell/shell.css`
- Modify: `test/deed.test.js`

**Interfaces:**
- 상태 `uploaded: { [pageKey]: fileId[] }` (Task 3에서 초기화·PROGRESS 등록됨).
- `uploadTo(page, fileId)`, `checkUploaded()`. objective 스펙 `upload: { file, page }`.

- [ ] **Step 1: 실패하는 테스트**

`test/deed.test.js` 끝에:

```js
// 드라이브 페이지에 올린 파일은 그 페이지 아래에 남는다. 올린 것이 눈에
// 보여야 한 일이 된다.
describe('드라이브에 올린다', () => {
  const SPEC = { id: 'deed_t_up', title: 't', grant: 'deed_t_up', upload: { file: 'file_qc', page: 'q3' } }
  beforeEach(() => useGame.setState({
    scenario: { ...scenario, objectives: [...scenario.objectives, SPEC] },
    grants: {}, uploaded: {}, pendingAsks: {}, extraMessages: {}
  }))

  it('올리면 페이지 목록에 붙고, 같은 파일은 한 번만', () => {
    useGame.getState().uploadTo('q3', 'file_qb')
    useGame.getState().uploadTo('q3', 'file_qb')
    expect(useGame.getState().uploaded.q3).toEqual(['file_qb'])
  })

  it('맞는 페이지에 맞는 파일이면 grant, 다른 페이지면 침묵', () => {
    useGame.getState().uploadTo('owner', 'file_qc')
    expect(useGame.getState().grants.deed_t_up).toBeUndefined()
    useGame.getState().uploadTo('q3', 'file_qc')
    expect(useGame.getState().grants.deed_t_up).toBe(true)
  })

  it('uploaded는 세이브에 실린다', async () => {
    const { PROGRESS } = await import('../src/engine/store.js')
    expect(PROGRESS).toContain('uploaded')
  })
})
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run test/deed.test.js`
Expected: `uploadTo is not a function`.

- [ ] **Step 3: store 액션**

`checkPlaced:` 액션 아래에:

```js
  uploadTo: (page, fileId) => {
    set((s) => ({ uploaded: { ...s.uploaded, [page]: [...new Set([...(s.uploaded[page] ?? []), fileId])] } }))
    get().checkUploaded()
  },
  checkUploaded: () => {
    const { scenario, uploaded, grants, grant } = get()
    scenario.objectives
      .filter((o) => o.upload && !grants[o.grant] && (uploaded[o.upload.page] ?? []).includes(o.upload.file))
      .forEach((o) => grant(o.grant))
  },
```

- [ ] **Step 4: 통과 확인**

Run: `npx vitest run test/deed.test.js`
Expected: 통과.

- [ ] **Step 5: Drive UI**

`src/apps/Drive.jsx`:

import에 추가: `import FileDialog from './FileDialog.jsx'`, `import { findFile, gameClock } from '../engine/store.js'` (기존 `useGame` import 줄에 합친다), 아이콘 `Upload`가 `../icons/line.jsx`에 있으면 쓰고 없으면 `FolderOpen`을 그대로 쓴다.

셀렉터 추가:
```js
  const uploaded = useGame((s) => s.uploaded)
  const uploadTo = useGame((s) => s.uploadTo)
  const scenario = useGame((s) => s.scenario)
  const clock = useGame((s) => gameClock(s.scenario, s))
  const [picking, setPicking] = useState(false)
  const mine = (uploaded[open] ?? []).map((id) => findFile(scenario.fs, id)).filter(Boolean)
```

`dr-crumb`의 `<span className="dr-crumb-gap" />` 바로 뒤에:
```jsx
          {open && (
            <button className="dr-upload" onClick={() => setPicking(true)}>
              <FolderOpen size={14} strokeWidth={2} />업로드
            </button>
          )}
```

`<WikiDoc site={site} id={open} />` 아래(같은 `<article>` 안)에:
```jsx
            {mine.length > 0 && (
              <table className="dr-table dr-uploaded">
                <thead><tr><th>올린 파일</th><th className="dr-col-who">올린 사람</th><th className="dr-col-when">올린 시각</th></tr></thead>
                <tbody>
                  {mine.map((f) => (
                    <tr key={f.id}>
                      <td><List size={16} strokeWidth={1.8} />{f.name}</td>
                      <td className="dr-col-who">{scenario.player.name}</td>
                      <td className="dr-col-when">{clock.date} {clock.time}</td>
                    </tr>
                  ))}
                </tbody>
              </table>
            )}
```

컴포넌트 return의 최상위 `<div className="dr">` 안 끝에:
```jsx
      {picking && (
        <FileDialog start="문서" onClose={() => setPicking(false)}
                    onPick={(file) => { uploadTo(open, file.id); setPicking(false) }} />
      )}
```

`FileDialog`의 `onPick`이 파일 객체를 넘기는지 확인: `src/apps/FileDialog.jsx:34` `if (chosen) onPick(chosen)` — `chosen`이 파일 객체면 `file.id`, id 문자열이면 그대로 쓴다. 확인 후 맞춘다.

`src/shell/shell.css` 끝에:
```css
/* 드라이브: 업로드 버튼과 올린 파일 표 */
.dr-upload { display: inline-flex; align-items: center; gap: 5px; padding: 5px 11px; border: 1px solid #c9ccd3; border-radius: 6px; background: #fff; font: inherit; font-size: 12px; cursor: pointer; }
.dr-upload:hover { background: #f3f4f6; }
.dr-uploaded { margin-top: 22px; }
.dr-uploaded td:first-child { display: flex; align-items: center; gap: 6px; }
```

`gameClock(scenario, {day, overtime, dayAt})` 서명은 store에 있다 — 셀렉터에서 `s` 전체를 넘기면 필요한 필드를 읽는다.

- [ ] **Step 6: 빌드·전체 테스트**

Run: `npm run build && npm test`
Expected: 통과. 개발 서버에서 드라이브(VPN 필요) 페이지를 열어 업로드 → 아래 표에 붙는지, 새로고침 뒤 남는지 본다.

- [ ] **Step 7: 커밋**

```bash
git add src/engine/store.js src/apps/Drive.jsx src/shell/shell.css test/deed.test.js
git commit -m "feat: 사내 드라이브에 파일을 올린다

페이지 상단의 업로드가 첨부에 쓰는 파일 고르기 창을 띄우고, 고른 파일이
페이지 아래 표에 붙는다. upload 목표는 그 페이지에 그 파일이 올라간 순간
켜진다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: 콘텐츠 16건 + 불변식

**Files:**
- Modify: `src/scenarios/workday.json` (스크립트로)
- Modify: `test/scenario.test.js:238-246`
- Modify: `test/deed.test.js` (스펙 불변식 describe)

**Interfaces:**
- Consumes: Task 1~4의 objective 스펙 모양.
- pool 요청 모양: `{ id, beat: { source:'workMessenger', thread, from, lines, ask: { deed: id, grants: id, placeholder, ok, no, next: [] } } }`, `pool.before[id]=3` 또는 `pool.after[id]=4`, `objectives.push({ id, title, grant: id, <spec> })`.

- [ ] **Step 1: 불변식 먼저 — 실패하는 테스트**

`test/scenario.test.js:238` 'every question wants either typed text or a real file, never neither'의 for 루프를:

```js
    for (const a of asks) {
      // 행동으로 푸는 질문은 칠 것도 낼 파일도 없다 — 기다리는 grant 만 있다
      if (a.deed) { expect(a.accept ?? a.files).toBeUndefined(); continue }
      expect(Boolean(a.accept?.length) !== Boolean(a.files?.length)).toBe(true)
      for (const id of a.files ?? []) expect(known.has(id)).toBe(true)
    }
```

`test/deed.test.js` 끝에:

```js
// 행동으로 푸는 요청은 accept 검사를 면제받는 대신 이것을 지킨다: 기다리는
// objective 가 검사 스펙을 갖고, 그 스펙이 가리키는 파일·폴더·페이지·주소가
// 실제로 게임 안에 있다. 하나라도 어긋나면 그 요청은 영영 못 푼다.
describe('행동 요청의 스펙', () => {
  const steps = (a) => (a ? [a, ...steps(a.then)] : [])
  const deeds = scenario.pool.requests.flatMap((r) => steps(r.beat.ask)).filter((a) => a.deed)
  const files = new Set(allFiles(scenario.fs).map((f) => f.id))
  const folders = new Set()
  const walk = (es, trail) => es.forEach((e) => { if (e.children) { folders.add(trail + '/' + e.name); walk(e.children, trail + '/' + e.name) } })
  Object.entries(scenario.fs).forEach(([r, es]) => { folders.add(r); walk(es, r) })
  const drive = scenario.sites.find((s) => s.layout === 'drive').wiki.pages
  const world = JSON.stringify(scenario)

  it('열여섯 건이 있고 종류당 넷이다', () => {
    expect(deeds.length).toBeGreaterThanOrEqual(16)
    const kinds = deeds.map((a) => {
      const o = scenario.objectives.find((x) => x.id === a.deed)
      return ['cell', 'mail', 'move', 'rename', 'upload'].find((k) => o?.[k])
    })
    for (const k of ['cell', 'mail', 'upload']) expect(kinds.filter((x) => x === k).length, k).toBeGreaterThanOrEqual(4)
    expect(kinds.filter((x) => x === 'move' || x === 'rename').length).toBeGreaterThanOrEqual(4)
    expect(kinds.every(Boolean)).toBe(true)
  })

  it('스펙이 가리키는 것이 다 실존한다', () => {
    for (const a of deeds) {
      const o = scenario.objectives.find((x) => x.id === a.deed)
      if (o.cell) {
        const sheet = allFiles(scenario.fs).find((f) => f.id === o.cell.file)?.sheets?.find((s) => s.name === o.cell.sheet)
        expect(sheet?.rows[o.cell.row]?.[o.cell.col], a.deed).toBeDefined()
        expect(sheet.rows[o.cell.row][o.cell.col], a.deed).not.toBe(o.cell.value)
      }
      if (o.mail) {
        expect(world, a.deed).toContain(o.mail.to)
        if (o.mail.requiredAttachment) { expect(files.has(o.mail.requiredAttachment), a.deed).toBe(true); expect(o.mail.wrongAttachmentReply, a.deed).toBeTruthy() }
        if (o.mail.requiredKeywords?.length) expect(o.mail.unclearReply, a.deed).toBeTruthy()
        expect(o.mail.reply?.body, a.deed).toBeTruthy()
      }
      if (o.move) { expect(files.has(o.move.file), a.deed).toBe(true); expect(folders.has(o.move.into), a.deed).toBe(true) }
      if (o.rename) { expect(files.has(o.rename.file), a.deed).toBe(true); expect(o.rename.name, a.deed).toMatch(/\.[a-z]+$/) }
      if (o.upload) { expect(files.has(o.upload.file), a.deed).toBe(true); expect(drive[o.upload.page], a.deed).toBeTruthy() }
    }
  })

  it('말할 것과 힌트가 있다', () => {
    for (const a of deeds) {
      expect(a.placeholder, a.deed).toBeTruthy()
      expect(a.ok?.length, a.deed).toBeGreaterThan(0)
      expect(a.no?.length, a.deed).toBe(3)
      expect(a.next, a.deed).toEqual([])
    }
  })

  it('옮기는 파일은 이미 받은 것이어야 한다', () => {
    // 메일 첨부(attached)는 저장하기 전엔 어디에도 없다. 그런 파일을 옮기라고
    // 하면 못 받은 판에서 영영 못 푼다.
    const attached = new Set(allFiles(scenario.fs).filter((f) => f.attached).map((f) => f.id))
    for (const a of deeds) {
      const o = scenario.objectives.find((x) => x.id === a.deed)
      for (const id of [o.move?.file, o.rename?.file, o.upload?.file, o.mail?.requiredAttachment].filter(Boolean)) {
        expect(attached.has(id), a.deed + ' ' + id).toBe(false)
      }
    }
  })
})
```

import 줄에 `allFiles` 추가: `import { allFiles, fsView, place, useGame } from '../src/engine/store.js'`.

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run test/deed.test.js`
Expected: '열여섯 건이 있고…' 실패 (0건).

- [ ] **Step 3: 콘텐츠 스크립트**

스크래치패드에 `deeds.mjs`를 쓰고 `node <경로>/deeds.mjs` 로 실행한다:

```js
import { readFileSync, writeFileSync } from 'node:fs'
const P = 'src/scenarios/workday.json'
const s = JSON.parse(readFileSync(P, 'utf8'))

const W = 'workMessenger'
const req = (id, thread, from, lines, ask, level) => {
  s.pool.requests.push({ id, beat: { source: W, thread, from, lines, ask: { deed: id, grants: id, next: [], ...ask } } })
  if (level === 'easy') s.pool.before[id] = 3
  else s.pool.after[id] = 4
}
const obj = (id, title, spec) => s.objectives.push({ id, title, grant: id, ...spec })
const R = (from, addr, body) => ({ from: `${from} <${addr}>`, subject: 'RE: {subject}', body })

// ── 셀 ────────────────────────────────────────────────────────────────
req('deed_cell_orders_c', 'boss', '박 팀장',
  ['김대리, 발주현황 시트 좀 손봐 줘요.', '문서 > 업무자료 > 2026의 거래처_발주현황.xlsx, 2026 시트에서 C테크 상태를 "납품 완료"로 바꾸고 저장해요.'],
  { placeholder: '거래처_발주현황.xlsx의 C테크 상태를 고쳐 저장하면 확인됩니다',
    ok: ['확인했어요. 시트가 바로 반영되네요.', '고마워요.'],
    no: [['아직 그대로예요. 저장했어요?', '고치고 나서 저장 버튼(또는 Ctrl+S)을 눌러야 반영돼요.'],
      ['C테크 줄이에요. 상태 칸만 바꾸면 돼요.'],
      ['문서 > 업무자료 > 2026 > 거래처_발주현황.xlsx, "2026" 시트의 C테크 행 "상태" 칸을 "납품 완료"로 고치고 저장이요.']] }, 'easy')
obj('deed_cell_orders_c', '발주현황 시트에서 C테크 상태 고치기', { cell: { file: 'file_xls_orders', sheet: '2026', row: 2, col: 3, value: '납품 완료' } })

req('deed_cell_stock_bw50', 'junho', '이준호',
  ['한별, 재고현황 하나만 고쳐 줘.', '업무자료 2026 폴더의 재고현황.xlsx에서 BW-50 입고예정을 70으로. 공장에서 20 더 보낸대.'],
  { placeholder: '재고현황.xlsx의 BW-50 입고예정을 고쳐 저장하면 확인됩니다',
    ok: ['됐다. 고맙다.'],
    no: [['아직 안 바뀐 것 같은데. 저장했어?'],
      ['BW-50 줄의 입고예정 칸이야. 현재고 말고.'],
      ['문서 > 업무자료 > 2026 > 재고현황.xlsx, "재고" 시트 BW-50 행의 "입고예정"을 70으로 고치고 저장.']] }, 'easy')
obj('deed_cell_stock_bw50', '재고현황 시트에서 BW-50 입고예정 고치기', { cell: { file: 'file_xls_stock', sheet: '재고', row: 1, col: 3, value: '70' } })

req('deed_cell_orders_b', 'boss', '박 팀장',
  ['김대리, 발주현황의 B물산 상태가 옛날 거예요.', '납품 일정표에 적힌 지금 상태로 맞춰서 저장해 둬요.'],
  { placeholder: '발주현황의 B물산 상태를 일정표대로 고쳐 저장하면 확인됩니다',
    ok: ['맞네요. 두 파일이 이제 같은 말을 하네요.'],
    no: [['아직이에요. 일정표에 뭐라고 적혀 있는지 먼저 봐요.'],
      ['납품 일정표는 업무자료 2026 폴더에 있어요. 거기 B물산 줄의 상태를 그대로 옮기면 돼요.'],
      ['문서 > 업무자료 > 2026 > 납품_일정_2026.xlsx의 B물산 "상태"를 보고, 거래처_발주현황.xlsx "2026" 시트 B물산 행의 "상태" 칸에 똑같이 적고 저장이요.']] }, 'hard')
obj('deed_cell_orders_b', '발주현황 시트의 B물산 상태를 일정표에 맞추기', { cell: { file: 'file_xls_orders', sheet: '2026', row: 1, col: 3, value: '출고 대기' } })

req('deed_cell_ws_sec', 'soyoung', '한소영',
  ['워크숍 참가 현황 네가 갖고 있지?', '정보보안팀 남은 한 명도 간대. 그 팀은 이제 전원 참가로 고쳐 줘.'],
  { placeholder: '워크숍 참가 현황의 정보보안팀 참가 확정을 고쳐 저장하면 확인됩니다',
    ok: ['오케이. 인사총무팀에 넘길게.'],
    no: [['아직 안 바뀌었는데. 저장은 했어?'],
      ['정보보안팀 줄이야. 재직 인원이 몇인지 같은 줄에 있잖아. 참가 확정을 그 수로.'],
      ['문서 > 업무자료 > 2026 > 워크숍_참가신청_현황.xlsx "부서별집계" 시트, 정보보안팀 행의 "참가 확정"을 "9명"으로 고치고 저장.']] }, 'hard')
obj('deed_cell_ws_sec', '워크숍 참가 현황에서 정보보안팀 전원 참가로 고치기', { cell: { file: 'file_c_wsapply', sheet: '부서별집계', row: 6, col: 2, value: '9명' } })

// ── 메일 ──────────────────────────────────────────────────────────────
req('deed_mail_qc', 'minseo', '최민서',
  ['선배님, C테크 김영민 대리님이 견적서를 메일로 다시 받고 싶으시대요.', '업무자료 2026 > C테크 폴더의 견적서_C테크.hwp를 ym.kim@ctech.co.kr로 보내 주시겠어요? 본문에 견적서라고 적어 주시고요.'],
  { placeholder: '견적서_C테크.hwp를 첨부해 김영민 대리에게 메일을 보내면 확인됩니다',
    ok: ['답장 왔대요! 감사합니다.'],
    no: [['아직 못 받으셨대요. 메일 앱에서 새 메일로 보내셔야 해요.'],
      ['받는 사람은 ym.kim@ctech.co.kr, 첨부는 C테크 폴더의 견적서예요.'],
      ['메일 > 새 메일: 받는 사람 ym.kim@ctech.co.kr, 첨부 문서 > 업무자료 > 2026 > C테크 > 견적서_C테크.hwp, 본문에 "견적서"를 넣어서요.']] }, 'easy')
obj('deed_mail_qc', 'C테크에 견적서 메일 보내기', { mail: {
  to: 'ym.kim@ctech.co.kr', requiredAttachment: 'file_qc', requiredKeywords: ['견적서'],
  reply: R('김영민', 'ym.kim@ctech.co.kr', '김한별 대리님, 견적서 잘 받았습니다.\n검토 후 연락드리겠습니다.\n\n김영민 드림'),
  wrongAttachmentReply: R('김영민', 'ym.kim@ctech.co.kr', '김한별 대리님, 첨부가 저희 견적서가 아닌 것 같습니다.\nC테크 앞으로 된 파일로 다시 부탁드립니다.\n\n김영민 드림'),
  unclearReply: R('김영민', 'ym.kim@ctech.co.kr', '김한별 대리님, 메일 감사합니다.\n어떤 자료를 보내신 건지 본문에 한 줄 적어 주시면 정리에 도움이 되겠습니다.\n\n김영민 드림') } })

req('deed_mail_form', 'junho', '이준호',
  ['한별, F상사 박정훈 씨가 우리 견적서 양식을 달래.', 'jh.park@fsangsa.co.kr로 공통양식 폴더의 견적서_양식.hwp 보내 줘. 본문에 양식이라고 적고.'],
  { placeholder: '견적서_양식.hwp를 첨부해 F상사에 메일을 보내면 확인됩니다',
    ok: ['받았대. 땡큐.'],
    no: [['아직 안 갔대. 새 메일로 보내야 해.'],
      ['받는 사람 jh.park@fsangsa.co.kr, 첨부는 공통양식에 있는 견적서 양식.'],
      ['메일 > 새 메일: jh.park@fsangsa.co.kr 앞으로, 문서 > 업무자료 > 2026 > 공통양식 > 견적서_양식.hwp 첨부, 본문에 "양식".']] }, 'easy')
obj('deed_mail_form', 'F상사에 견적서 양식 보내기', { mail: {
  to: 'jh.park@fsangsa.co.kr', requiredAttachment: 'file_form_quote', requiredKeywords: ['양식'],
  reply: R('박정훈', 'jh.park@fsangsa.co.kr', '김한별 대리님, 양식 잘 받았습니다. 감사합니다.\n\n박정훈 드림'),
  wrongAttachmentReply: R('박정훈', 'jh.park@fsangsa.co.kr', '김한별 대리님, 첨부가 양식 파일이 아닌 것 같습니다.\n다시 한번 부탁드립니다.\n\n박정훈 드림'),
  unclearReply: R('박정훈', 'jh.park@fsangsa.co.kr', '김한별 대리님, 무엇을 보내신 건지 본문에 적어 주시면 감사하겠습니다.\n\n박정훈 드림') } })

req('deed_mail_qd', 'boss', '박 팀장',
  ['김대리, D유통 담당자한테 견적서를 메일로 한 부 보내 줘요.', '주소는 우리가 받은 문서에 있을 거예요. 본문에 견적서라고 밝히고요.'],
  { placeholder: 'D유통 담당자에게 견적서_D유통.hwp를 첨부해 메일을 보내면 확인됩니다',
    ok: ['D유통에서 받았다고 연락 왔어요. 수고했어요.'],
    no: [['아직이에요. D유통 담당자 주소부터 찾아요.'],
      ['D유통 폴더의 검토 문서나 계약서 발송대장에 담당자 메일이 적혀 있어요. 첨부는 초안 말고 최종 견적서요.'],
      ['메일 > 새 메일: sa.yoon@dyutong.co.kr 앞으로, 문서 > 업무자료 > 2026 > D유통 > 견적서_D유통.hwp 첨부, 본문에 "견적서".']] }, 'hard')
obj('deed_mail_qd', 'D유통 담당자에게 견적서 메일 보내기', { mail: {
  to: 'sa.yoon@dyutong.co.kr', requiredAttachment: 'file_qd', requiredKeywords: ['견적서'],
  reply: R('윤성아', 'sa.yoon@dyutong.co.kr', '김한별 대리님, 견적서 잘 받았습니다.\n내부 검토 후 회신드리겠습니다.\n\n윤성아 드림'),
  wrongAttachmentReply: R('윤성아', 'sa.yoon@dyutong.co.kr', '김한별 대리님, 첨부하신 파일이 초안이거나 저희 것이 아닌 것 같습니다.\n최종 견적서로 다시 부탁드립니다.\n\n윤성아 드림'),
  unclearReply: R('윤성아', 'sa.yoon@dyutong.co.kr', '김한별 대리님, 어떤 문서를 보내신 건지 본문에 적어 주시면 감사하겠습니다.\n\n윤성아 드림') } })

req('deed_mail_gsan', 'soyoung', '한소영',
  ['G산업 쪽에서 지난번 회의록 사본 달래.', '담당자 주소는 회의록에 적혀 있을 거야. 본문에 회의록이라고 써 줘.'],
  { placeholder: 'G산업 담당자에게 회의록을 첨부해 메일을 보내면 확인됩니다',
    ok: ['받았대. 고마워.'],
    no: [['아직 안 갔대. 회의록 파일부터 찾아.'],
      ['그 회의록은 엉뚱한 폴더에 저장돼 있어 — 업무자료 바로 아래 "새 폴더". 담당자 주소도 그 안에 있어.'],
      ['메일 > 새 메일: jh.han@gsanup.co.kr 앞으로, 문서 > 업무자료 > 새 폴더 > 회의록_20260812.hwp 첨부, 본문에 "회의록".']] }, 'hard')
obj('deed_mail_gsan', 'G산업 담당자에게 회의록 메일 보내기', { mail: {
  to: 'jh.han@gsanup.co.kr', requiredAttachment: 'file_h_gminutes', requiredKeywords: ['회의록'],
  reply: R('한지호', 'jh.han@gsanup.co.kr', '김한별 대리님, 회의록 잘 받았습니다. 감사합니다.\n\n한지호 드림'),
  wrongAttachmentReply: R('한지호', 'jh.han@gsanup.co.kr', '김한별 대리님, 첨부가 지난 회의록이 아닌 것 같습니다.\n다시 부탁드립니다.\n\n한지호 드림'),
  unclearReply: R('한지호', 'jh.han@gsanup.co.kr', '김한별 대리님, 무엇을 보내신 건지 본문에 적어 주시면 감사하겠습니다.\n\n한지호 드림') } })

// ── 이동 · 이름 ───────────────────────────────────────────────────────
req('deed_move_ppt', 'boss', '박 팀장',
  ['김대리, 다운로드 폴더에 회의자료_최종.pptx 굴러다니죠?', '문서 > 발표자료로 옮겨 놔요. 탐색기에서 잘라내서 붙여 넣으면 돼요.'],
  { placeholder: '회의자료_최종.pptx를 문서 > 발표자료로 옮기면 확인됩니다',
    ok: ['정리됐네요. 고마워요.'],
    no: [['아직 다운로드에 있어요. 우클릭 > 잘라내기, 목적 폴더 빈자리에서 우클릭 > 붙여넣기예요.'],
      ['파일은 다운로드에, 목적지는 문서 안의 발표자료 폴더예요.'],
      ['다운로드 > 회의자료_최종.pptx 우클릭 > 잘라내기, 그다음 문서 > 발표자료 빈자리 우클릭 > 붙여넣기요.']] }, 'easy')
obj('deed_move_ppt', '회의자료_최종.pptx를 발표자료 폴더로 옮기기', { move: { file: 'file_ppt_meeting', into: '문서/발표자료' } })

req('deed_rename_ppt', 'minseo', '최민서',
  ['선배님, 다운로드에 회의자료_최종_final.pptx요, 이름이 너무 헷갈려요.', '회의자료_0825로 이름 바꿔 주시면 안 될까요? 확장자는 그대로요.'],
  { placeholder: '회의자료_최종_final.pptx의 이름을 회의자료_0825로 바꾸면 확인됩니다',
    ok: ['훨씬 낫네요! 감사합니다.'],
    no: [['아직 그대로예요. 파일 우클릭에 이름 바꾸기가 있어요.'],
      ['다운로드 폴더의 회의자료_최종_final.pptx예요. 이름만 회의자료_0825로요.'],
      ['다운로드 > 회의자료_최종_final.pptx 우클릭 > 이름 바꾸기 > "회의자료_0825" 입력 후 Enter요. 확장자 .pptx는 자동으로 붙어요.']] }, 'easy')
obj('deed_rename_ppt', '회의자료_최종_final.pptx 이름 바꾸기', { rename: { file: 'file_dummy_082', name: '회의자료_0825.pptx' } })

req('deed_move_gminutes', 'junho', '이준호',
  ['한별, 지난주 G산업 회의록 어디 엉뚱한 데 저장해 둔 것 같은데.', 'G산업 폴더에 있어야 할 거 아냐. 제자리로 옮겨 줘.'],
  { placeholder: 'G산업 회의록을 G산업 폴더로 옮기면 확인됩니다',
    ok: ['찾았네. 이제 제자리다.'],
    no: [['아직 G산업 폴더에 없는데. 회의록부터 찾아 봐.'],
      ['업무자료 바로 아래 "새 폴더"라는 게 하나 있어. 거기 들어 있을 거야.'],
      ['문서 > 업무자료 > 새 폴더 > 회의록_20260812.hwp 우클릭 > 잘라내기, 문서 > 업무자료 > 2026 > G산업 빈자리 우클릭 > 붙여넣기.']] }, 'hard')
obj('deed_move_gminutes', 'G산업 회의록을 G산업 폴더로 옮기기', { move: { file: 'file_h_gminutes', into: '문서/업무자료/2026/G산업' } })

req('deed_move_wsplan', 'soyoung', '한소영',
  ['워크숍 진행표 네가 받아 놨지? 다운로드에 두지 말고 워크샵 자료 모아 두는 데로 옮겨 줘.'],
  { placeholder: '워크숍_진행표.hwp를 개인 > 워크샵 폴더로 옮기면 확인됩니다',
    ok: ['됐다. 나중에 찾기 편하겠네.'],
    no: [['아직 다운로드에 있어.'],
      ['문서 > 개인 아래에 워크샵 폴더가 있어. 거기.'],
      ['다운로드 > 워크숍_진행표.hwp 우클릭 > 잘라내기, 문서 > 개인 > 워크샵 빈자리 우클릭 > 붙여넣기.']] }, 'hard')
obj('deed_move_wsplan', '워크숍 진행표를 워크샵 폴더로 옮기기', { move: { file: 'file_p2_wsplan', into: '문서/개인/워크샵' } })

// ── 업로드 ────────────────────────────────────────────────────────────
req('deed_up_equote', 'boss', '박 팀장',
  ['김대리, E마트 재거래 견적서 사내 드라이브에도 올려 둬요.', '영업1팀 공유 > 3분기 실적자료 페이지에 업로드 버튼 있어요.'],
  { placeholder: '드라이브 3분기 실적자료 페이지에 견적서_E마트_재거래.hwp를 올리면 확인됩니다',
    ok: ['올라왔네요. 팀에서 다 볼 수 있겠어요.'],
    no: [['아직 안 보여요. 드라이브는 VPN 켜고 들어가야 해요.'],
      ['드라이브 영업1팀 공유 폴더의 3분기 실적자료 페이지를 열면 위에 업로드가 있어요. 파일은 E마트 폴더에요.'],
      ['drive.ar.local > 영업1팀 공유 > 3분기 실적자료 > 업로드 > 문서 > 업무자료 > 2026 > E마트 > 견적서_E마트_재거래.hwp.']] }, 'easy')
obj('deed_up_equote', 'E마트 재거래 견적서 드라이브에 올리기', { upload: { file: 'file_e_quote', page: 'q3' } })

req('deed_up_bminutes', 'minseo', '최민서',
  ['선배님, B물산 납기 조율한 회의록 드라이브에 올려 주실 수 있어요?', '거래처 납품 이력 페이지요. B물산 폴더에 8월 5일자 회의록 있어요.'],
  { placeholder: '드라이브 거래처 납품 이력 페이지에 회의록_20260805.hwp를 올리면 확인됩니다',
    ok: ['확인했어요! 감사합니다.'],
    no: [['아직 안 올라왔어요. 드라이브 들어가셨어요?'],
      ['영업1팀 공유 > 거래처 납품 이력 페이지의 업로드요. 파일은 B물산 폴더에 있어요.'],
      ['drive.ar.local > 영업1팀 공유 > 거래처 납품 이력 > 업로드 > 문서 > 업무자료 > 2026 > B물산 > 회의록_20260805.hwp.']] }, 'easy')
obj('deed_up_bminutes', 'B물산 회의록 드라이브에 올리기', { upload: { file: 'file_bminutes', page: 'delivery' } })

req('deed_up_sendbook', 'junho', '이준호',
  ['한별, 계약서 발송대장 드라이브에 올려 줘. 계약 관련 페이지에.'],
  { placeholder: '드라이브의 계약 관련 페이지에 계약서_발송대장.hwp를 올리면 확인됩니다',
    ok: ['올라왔다. 고맙다.'],
    no: [['아직 안 보여. 드라이브 페이지 열어서 업로드 눌러야 해.'],
      ['발송대장은 D유통 폴더에 있어. 드라이브에서 계약이라는 말이 붙은 페이지를 찾아.'],
      ['drive.ar.local에서 제목에 계약이 들어간 페이지(주소창에 /contracts) > 업로드 > 문서 > 업무자료 > 2026 > D유통 > 계약서_발송대장.hwp.']] }, 'hard')
obj('deed_up_sendbook', '계약서 발송대장 드라이브에 올리기', { upload: { file: 'file_d_sendbook', page: 'contracts' } })

req('deed_up_creview', 'soyoung', '한소영',
  ['C테크 신규거래 검토한 거 드라이브에 올려 둬. 담당 이력 페이지.'],
  { placeholder: '드라이브 거래처 담당 이력 페이지에 신규거래_검토.hwp를 올리면 확인됩니다',
    ok: ['확인. 고마워.'],
    no: [['아직 안 올라왔어.'],
      ['영업1팀 공유 > 거래처 담당 이력 페이지. 파일은 C테크 폴더.'],
      ['drive.ar.local > 영업1팀 공유 > 거래처 담당 이력 > 업로드 > 문서 > 업무자료 > 2026 > C테크 > 신규거래_검토.hwp.']] }, 'hard')
obj('deed_up_creview', 'C테크 신규거래 검토서 드라이브에 올리기', { upload: { file: 'file_ctech_review', page: 'owner' } })

writeFileSync(P, JSON.stringify(s, null, 2) + '\n')
console.log('pool', s.pool.requests.length, 'objectives', s.objectives.length)
```

실행 전 확인: `node scripts/query.mjs find contracts` 로 드라이브에 `contracts` 페이지가 있는지(있다: `pages: … contracts …`). `file_h_gminutes`가 `attached`가 아닌지 — 스크립트 실행 후 '옮기는 파일은 이미 받은 것이어야 한다' 테스트가 잡는다.

- [ ] **Step 4: 실행·테스트**

Run: `node <scratchpad>/deeds.mjs && npm test`
Expected: `pool 164 objectives ...`, 전부 통과. 실패하면 메시지가 어느 요청·어느 필드인지 말해 준다 — 콘텐츠를 고치고 다시 실행(스크립트는 push라 두 번 돌리면 중복된다: 다시 돌리기 전에 `git checkout src/scenarios/workday.json`).

흔한 실패:
- `pool.test` 'holds about twice…' — 통과해야 정상(늘었으니).
- `hidden.test` — 새 요청이 숨김 파일을 가리키면 마지막 힌트에 '숨긴 항목' 필요. 위 16건은 숨김 파일을 안 쓴다.
- `chatter`/누출 테스트 — 새 대사에 accept 문자열이 있으면 어느 것인지 알려 준다. 그 낱말을 바꾼다.
- `friday.test` 날짜·요일 — 새 대사에 날짜 없음.

- [ ] **Step 5: 커밋**

```bash
git add src/scenarios/workday.json test/scenario.test.js test/deed.test.js
git commit -m "feat: 행동으로 푸는 요청 열여섯 건

셀 넷 · 메일 넷 · 이동/이름 넷 · 업로드 넷. 절반은 어디서 무엇을 하면
되는지 말해 주고(2~3일차), 절반은 값·주소·파일을 게임 안에서 찾아와야
한다(4~5일차). 기존 요청은 그대로다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: 문서와 조회 도구

**Files:**
- Modify: `scripts/query.mjs` (`ask` 출력)
- Modify: `CLAUDE.md` (시나리오 불변 규칙에 한 줄)

- [ ] **Step 1: query.mjs의 `pool` 목록에 종류 표시**

`scripts/query.mjs`에서 `pool:` 명령을 찾아, 각 요청 줄에 `r.beat.ask.deed ? '[행동]' : ''` 를 덧붙인다. 예:

```js
  pool: () => s.pool.requests.forEach((r) => console.log(`${r.id}\t${r.beat.thread}\t${steps(r.beat.ask).length}단계\t${r.beat.ask.deed ? '행동' : ''}\t${s.pool.after[r.id] ? 'day>=' + s.pool.after[r.id] : ''}`)),
```
(기존 출력 형식은 유지하고 한 열만 더한다.)

- [ ] **Step 2: CLAUDE.md**

"시나리오 데이터 불변 규칙" 목록 끝에:

```
- **행동으로 푸는 ask**: `deed: <objective id>` (+ 마지막 단계면 `grants` 같은 값). accept 검사 면제 대신 그 objective에 `cell|mail|move|rename|upload` 스펙이 있어야 하고, 가리키는 파일·폴더·페이지·주소가 실존해야 한다 (`test/deed.test.js`). `attached` 파일은 쓰지 마라.
```

- [ ] **Step 3: 전체 확인·커밋**

Run: `node scripts/query.mjs pool | grep 행동 | wc -l && npm test && npm run build`
Expected: 16, 전부 통과, build ✓.

```bash
git add scripts/query.mjs CLAUDE.md
git commit -m "docs: 행동 요청의 규칙과 조회

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## 자기 점검

- 스펙 §1 `ask.deed` → Task 1. 메신저 안내문·힌트 버튼·창 닫혀도 진행 → Task 1.
- §2 셀(있음) / 메일 → Task 2 / 이동·이름 → Task 3 / 업로드 → Task 4. 세이브 → Task 3 PROGRESS.
- §3 콘텐츠 16건, before/after 배분 → Task 5.
- §4 불변식·엔진 테스트 → Task 1·2·3·4·5의 `deed.test.js`, `scenario.test.js` 면제.
- 스펙에서 뺀 것(의도적): 단축키 Ctrl+X/V·F2, 폰 셸 ⋯ 메뉴. 우클릭 메뉴만으로 완결되고, 필요하면 나중에 붙인다.
